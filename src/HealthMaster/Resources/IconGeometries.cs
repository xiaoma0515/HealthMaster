namespace HealthMaster.Resources;

/// <summary>
/// 四类提醒图标的矢量路径数据（WPF Path Mini-Language，统一 24×24 视框）。
/// 取代 v1.1 的 emoji 字形：WPF 不渲染彩色 emoji，四个字形在 17px 下视觉重量严重不均；
/// 改为自绘路径后线宽 / 留白 / 光学尺寸可统一，且**仍不引入任何外部图片或字体文件**。
/// 全部为纯填充（Fill）造型，不依赖描边，绑定端只需一个 <c>Path.Data</c> + <c>Fill</c>。
/// 前缀 F0 = EvenOdd（用于挖空），F1 = Nonzero。
/// </summary>
public static class IconGeometries
{
    /// <summary>护眼：杏仁眼廓（外轮廓 - 内轮廓 = 环）+ 实心瞳孔，靠 EvenOdd 挖空。</summary>
    public const string Eye =
        "F0 " +
        "M2,12 C5,6.5 8.6,4.5 12,4.5 C15.4,4.5 19,6.5 22,12 " +
        "C19,17.5 15.4,19.5 12,19.5 C8.6,19.5 5,17.5 2,12 Z " +
        "M4.6,12 C7,8.4 9.6,6.6 12,6.6 C14.4,6.6 17,8.4 19.4,12 " +
        "C17,15.6 14.4,17.4 12,17.4 C9.6,17.4 7,15.6 4.6,12 Z " +
        "M8.9,12 A3.1,3.1 0 1 0 15.1,12 A3.1,3.1 0 1 0 8.9,12 Z";

    /// <summary>久坐：人形剪影（头 + 肩身），提示起身活动。</summary>
    public const string Sedentary =
        "F1 " +
        "M9,6.2 A3,3 0 1 0 15,6.2 A3,3 0 1 0 9,6.2 Z " +
        "M12,10.8 C15.6,10.8 18.5,13.4 18.5,16.6 L18.5,19.2 " +
        "C18.5,19.9 17.9,20.5 17.2,20.5 L6.8,20.5 " +
        "C6.1,20.5 5.5,19.9 5.5,19.2 L5.5,16.6 C5.5,13.4 8.4,10.8 12,10.8 Z";

    /// <summary>补水：水滴（尖顶 + 半圆底）。</summary>
    public const string Water =
        "F1 " +
        "M12,2.8 C12,2.8 5,10.6 5,15 A7,7 0 0 0 19,15 C19,10.6 12,2.8 12,2.8 Z";

    /// <summary>
    /// 运动：哑铃（中杆 + 两端圆角配重，三段互不重叠）。
    /// 配重加高到 10.8 单位、中杆加厚到 2.8 单位：Path 用 Stretch="Uniform"，
    /// 会按各自包围盒缩放到 18px 见方，原先 17×8.4 的扁包围盒会让哑铃在四类里显得又小又细；
    /// 现为 17×10.8，与另外三类的光学重量拉平，且中杆在 100% DPI 下仍有 ~2.1px 实心宽度。
    /// </summary>
    public const string Exercise =
        "F1 " +
        "M5.1,6.6 h1.8 a1.6,1.6 0 0 1 1.6,1.6 v7.6 a1.6,1.6 0 0 1 -1.6,1.6 " +
        "h-1.8 a1.6,1.6 0 0 1 -1.6,-1.6 v-7.6 a1.6,1.6 0 0 1 1.6,-1.6 Z " +
        "M17.1,6.6 h1.8 a1.6,1.6 0 0 1 1.6,1.6 v7.6 a1.6,1.6 0 0 1 -1.6,1.6 " +
        "h-1.8 a1.6,1.6 0 0 1 -1.6,-1.6 v-7.6 a1.6,1.6 0 0 1 1.6,-1.6 Z " +
        "M8.5,10.7 L15.5,10.7 L15.5,13.3 L8.5,13.3 Z";
}
